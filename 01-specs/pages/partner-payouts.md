# Partner Payouts — `/partner/payouts`

See the full spec at `instructor-payouts.md`. The implementation at `/partner/payouts` is the URL alias; the spec body is the same (the team unified on `/partner/*` as the URL prefix; the spec kept the legacy `instructor-*` name for history).

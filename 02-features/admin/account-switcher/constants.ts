// Shared constants for the account-switcher feature. Extracted so
// client + server agree on the same minimum/maximum reason lengths
// without hard-coding them in two places.

export const MIN_REASON_LEN = 20
export const MAX_REASON_LEN = 500
